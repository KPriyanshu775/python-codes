# 200+ Python Practice Questions — Easy to Hard

Work through these in order. Each section builds on the last.

---

## Section 1: Absolute Basics — Variables, I/O, Types (1–20)

1. Print "Hello, World!" to the console.
2. Store your name in a variable and print a greeting using it.
3. Take a number as input and print its type using `type()`.
4. Take two numbers as input and print their sum, difference, product, and quotient.
5. Convert a string input into an integer and print it.
6. Swap two variables' values without using a third variable.
7. Print the value of `10 // 3` and `10 % 3` and explain the difference.
8. Check the size in bytes of an integer using `sys.getsizeof()`.
9. Concatenate two strings and print the result.
10. Use an f-string to print `"My name is X and I am Y years old"`.
11. Convert Celsius to Fahrenheit given a temperature input.
12. Calculate the area and perimeter of a rectangle given length and width.
13. Calculate simple interest given principal, rate, and time.
14. Take a float input and round it to 2 decimal places.
15. Print the ASCII value of a character using `ord()`.
16. Print the character for a given ASCII value using `chr()`.
17. Take three inputs (name, age, city) and print them in one formatted sentence.
18. Explain the difference between `=` and `==` with an example.
19. Declare a variable and reassign it to a different data type. Print its type each time.
20. Write a one-line program to print numbers 1 to 5 separated by commas.

---

## Section 2: Operators & Conditionals (21–40)

21. Check if a number is even or odd.
22. Check if a number is positive, negative, or zero.
23. Find the largest of three numbers using if-elif-else.
24. Find the smallest of three numbers.
25. Check if a year is a leap year.
26. Check if a character is a vowel or consonant.
27. Check if a number is divisible by both 3 and 5.
28. Given marks, assign a grade (A/B/C/D/F) using if-elif.
29. Check if a triangle is valid given three side lengths.
30. Determine the type of triangle (equilateral, isosceles, scalene) given three sides.
31. Check if a given year is a century year and also a leap year.
32. Write a program using ternary conditional expression to find the max of two numbers.
33. Check if a number is a multiple of 4 but not of 100, unless also a multiple of 400.
34. Use `and`, `or`, `not` to check if a number lies between 10 and 20.
35. Take a person's age and classify them as child, teenager, adult, or senior.
36. Check if a given character is uppercase, lowercase, digit, or special character.
37. Check whether a given number is a perfect square.
38. Write a BMI calculator that also prints the health category.
39. Given three numbers, print them in ascending order without using `sorted()`.
40. Simulate a basic calculator (+, -, *, /) using if-elif based on user choice.

---

## Section 3: Loops (41–60)

41. Print numbers from 1 to 10 using a `for` loop.
42. Print numbers from 10 to 1 using a `while` loop.
43. Print all even numbers between 1 and 50.
44. Calculate the sum of first N natural numbers.
45. Calculate the factorial of a number using a loop.
46. Print the multiplication table of a given number.
47. Print all prime numbers between 1 and 100.
48. Find the sum of digits of a number.
49. Reverse a number (e.g., 1234 → 4321).
50. Check if a number is a palindrome (e.g., 121).
51. Check if a number is an Armstrong number (e.g., 153 = 1³+5³+3³).
52. Print the Fibonacci series up to N terms using a loop.
53. Find the GCD of two numbers using a loop.
54. Find the LCM of two numbers.
55. Print a right-angled triangle pattern of stars.
56. Print a pyramid pattern of stars.
57. Print a number pattern (e.g., Pascal's triangle, first 5 rows).
58. Count the number of digits in a number.
59. Find the sum of all prime numbers below 1000.
60. Print all Armstrong numbers between 1 and 1000.

---

## Section 4: Strings (61–85)

61. Find the length of a string without using `len()`.
62. Reverse a string without using slicing or `reversed()`.
63. Check if a string is a palindrome.
64. Count the number of vowels and consonants in a string.
65. Count the occurrences of each character in a string.
66. Check if two strings are anagrams of each other.
67. Remove all whitespace from a string.
68. Convert a string to uppercase and lowercase without using built-in methods.
69. Check if a string contains only digits.
70. Find the first non-repeating character in a string.
71. Count the number of words in a sentence.
72. Capitalize the first letter of every word in a sentence.
73. Check if a string starts and ends with the same character.
74. Replace all occurrences of a character in a string without using `replace()`.
75. Find all substrings of a given string.
76. Check if one string is a rotation of another (e.g., "abcd" and "cdab").
77. Remove duplicate characters from a string while preserving order.
78. Find the longest word in a sentence.
79. Write a function to convert a string to title case manually.
80. Check if a string has balanced parentheses.
81. Compress a string using counts of repeated characters (e.g., "aaabb" → "a3b2").
82. Find the longest common prefix among a list of strings.
83. Implement a basic Caesar cipher (shift each letter by N).
84. Check if a string is a valid IPv4 address.
85. Given a paragraph, find the most frequently occurring word.

---

## Section 5: Lists & Tuples (86–110)

86. Find the sum and average of elements in a list.
87. Find the maximum and minimum in a list without using `max()`/`min()`.
88. Reverse a list without using `reverse()` or slicing.
89. Remove duplicates from a list while preserving order.
90. Find the second largest element in a list.
91. Count the occurrences of each element in a list.
92. Merge two sorted lists into one sorted list.
93. Find the intersection of two lists.
94. Find the union of two lists without duplicates.
95. Rotate a list to the left by N positions.
96. Find all pairs in a list that sum to a target value.
97. Flatten a nested list (e.g., `[1,[2,3,[4,5]],6]`).
98. Find the missing number in a list of 1 to N.
99. Check if a list is sorted.
100. Sort a list without using `sort()` or `sorted()` (implement bubble sort).
101. Implement selection sort on a list.
102. Implement insertion sort on a list.
103. Find the majority element in a list (appears more than n/2 times).
104. Split a list into chunks of size N.
105. Find the difference between two lists (elements in one but not the other).
106. Given a list of tuples, sort it by the second element.
107. Convert a list of tuples into a dictionary.
108. Find the index of the first occurrence of the maximum element.
109. Remove all occurrences of a given value from a list, in-place.
110. Given a list of numbers, separate even and odd numbers into two lists.

---

## Section 6: Dictionaries & Sets (111–130)

111. Create a dictionary from two lists (keys and values).
112. Merge two dictionaries.
113. Find the key with the maximum value in a dictionary.
114. Count the frequency of each word in a sentence using a dictionary.
115. Invert a dictionary (swap keys and values).
116. Sort a dictionary by its values.
117. Remove a key from a dictionary safely (handle the case it doesn't exist).
118. Find common keys between two dictionaries.
119. Check if two dictionaries are equal.
120. Given a list of student dictionaries (name, grade), find the topper.
121. Group a list of numbers into even and odd using a dictionary.
122. Find the union and intersection of two sets.
123. Check if one set is a subset of another.
124. Remove duplicates from a list using a set, then restore original order.
125. Find all unique characters across two strings using sets.
126. Use a dictionary to count character frequency and find the most common character.
127. Build a simple phonebook (dictionary) with add, update, delete, and search operations.
128. Convert a nested dictionary into a flat dictionary with dotted keys.
129. Given a dictionary of item:price, find items within a given budget.
130. Implement a word-frequency counter that ignores case and punctuation.

---

## Section 7: Functions (131–150)

131. Write a function to check if a number is prime.
132. Write a function that returns the factorial of a number (iterative).
133. Write a function with default arguments (e.g., `greet(name, greeting="Hello")`).
134. Write a function that accepts a variable number of arguments (`*args`).
135. Write a function that accepts keyword arguments (`**kwargs`).
136. Write a function that returns multiple values (as a tuple).
137. Write a lambda function to square a number.
138. Use `map()` to square all elements of a list.
139. Use `filter()` to get all even numbers from a list.
140. Use `reduce()` to find the product of all elements in a list.
141. Write a function that takes another function as an argument (higher-order function).
142. Write a decorator that prints the execution time of a function.
143. Write a decorator that logs function name and arguments each time it's called.
144. Write a generator function that yields Fibonacci numbers indefinitely.
145. Write a generator that yields only even squares up to N.
146. Explain and demonstrate the difference between `*args` and `**kwargs`.
147. Write a function with type hints and a docstring, then use `help()` on it.
148. Implement memoization manually using a dictionary inside a function.
149. Use `functools.lru_cache` to optimize a recursive Fibonacci function.
150. Write a function that validates an email address using regex.

---

## Section 8: Recursion (151–160)

151. Write a recursive function to calculate factorial.
152. Write a recursive function to calculate the sum of digits of a number.
153. Write a recursive function to reverse a string.
154. Write a recursive function to compute the nth Fibonacci number.
155. Write a recursive function to check if a string is a palindrome.
156. Write a recursive function to find the GCD of two numbers.
157. Write a recursive function to calculate power (base^exponent).
158. Write a recursive function to flatten a nested list.
159. Write a recursive function to compute the sum of a list.
160. Solve the Tower of Hanoi problem recursively and print the steps.

---

## Section 9: File Handling & Exceptions (161–170)

161. Write text to a file and read it back.
162. Count the number of lines, words, and characters in a text file.
163. Append data to an existing file without overwriting it.
164. Read a file and print only lines containing a specific keyword.
165. Copy the contents of one file into another.
166. Handle a `ZeroDivisionError` using try-except.
167. Handle multiple exception types (e.g., `ValueError`, `TypeError`) in one program.
168. Use `finally` to ensure a file is closed even if an error occurs.
169. Create and raise a custom exception class.
170. Read a CSV file (without pandas) and print it as a list of dictionaries.

---

## Section 10: Object-Oriented Programming (171–195)

171. Create a `Person` class with `name` and `age` attributes and a method to introduce itself.
172. Create a `Rectangle` class with methods to compute area and perimeter.
173. Demonstrate the difference between instance variables and class variables.
174. Create a class with a class method and a static method, and explain the difference.
175. Implement inheritance: create a `Student` class that inherits from `Person`.
176. Demonstrate method overriding in a subclass.
177. Use `super()` to call a parent class's constructor.
178. Implement multiple inheritance and explain the MRO (Method Resolution Order).
179. Create an abstract base class using the `abc` module with an abstract method.
180. Implement operator overloading (e.g., override `__add__` for a `Vector` class).
181. Implement `__str__` and `__repr__` for a custom class.
182. Implement `__eq__` and `__lt__` to make objects comparable.
183. Create a `BankAccount` class with deposit, withdraw, and balance-check methods, raising exceptions for invalid operations.
184. Implement encapsulation using private attributes (`__balance`) and property getters/setters.
185. Create a `Stack` class with `push`, `pop`, `peek`, and `is_empty` methods.
186. Create a `Queue` class using a list, with `enqueue` and `dequeue` methods.
187. Implement a `Shape` class hierarchy (`Circle`, `Square`, `Triangle`) with polymorphic `area()` methods.
188. Use `@property` to create a computed attribute (e.g., `full_name` from `first_name` + `last_name`).
189. Implement a custom iterator class using `__iter__` and `__next__`.
190. Implement a context manager class using `__enter__` and `__exit__`.
191. Create a `LinkedList` class with methods to insert, delete, and print nodes.
192. Implement a `BinaryTree` class with insert and in-order traversal methods.
193. Create a singleton class (only one instance can ever be created).
194. Design a simple `Library` system with `Book` and `Member` classes supporting borrow/return.
195. Implement a `__len__` and `__getitem__` on a custom class to make it behave like a list.

---

## Section 11: Algorithms (196–215)

196. Implement linear search.
197. Implement binary search (iterative and recursive).
198. Implement bubble sort and analyze its time complexity.
199. Implement merge sort.
200. Implement quicksort.
201. Implement a function to check if brackets in an expression are balanced (using a stack).
202. Find the first duplicate element in an array.
203. Find the missing and repeating number in an array of 1 to N.
204. Implement a two-pointer solution to find a pair with a given sum in a sorted array.
205. Implement the sliding window technique to find the maximum sum subarray of size K.
206. Find the longest increasing subsequence in an array.
207. Implement Kadane's algorithm to find the maximum subarray sum.
208. Detect a cycle in a linked list (Floyd's algorithm).
209. Implement depth-first search (DFS) on a graph represented as an adjacency list.
210. Implement breadth-first search (BFS) on a graph.
211. Implement Dijkstra's algorithm for shortest paths.
212. Solve the N-Queens problem using backtracking.
213. Solve a Sudoku puzzle using backtracking.
214. Implement dynamic programming to solve the 0/1 Knapsack problem.
215. Implement dynamic programming to find the longest common subsequence between two strings.

---

## Section 12: Advanced / Real-World (216–225)

216. Implement an LRU (Least Recently Used) cache using `OrderedDict` or a dictionary + doubly linked list.
217. Write a multithreaded program that downloads/processes multiple items concurrently.
218. Write a program using `asyncio` to run two coroutines concurrently.
219. Use `argparse` to build a simple command-line tool that takes flags and arguments.
220. Use regex to extract all email addresses from a block of text.
221. Write a script that reads a large file line-by-line (memory-efficient) and counts word frequency, printing the top 5.
222. Implement a basic rate limiter (e.g., token bucket) as a decorator.
223. Write a simple key-value store class that persists data to a JSON file.
224. Implement a producer-consumer pattern using `queue.Queue` and threads.
225. Design and implement a small CLI-based Tic-Tac-Toe game with a simple AI opponent (minimax).

---

**How to use this:** work through a section at a time. Once you can do most of a section without help, move to the next. Send me your solution to any question and I'll review it, or ask me for a hint if you're stuck.
